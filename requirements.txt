opencv-python>=4.8
numpy>=1.24
torch>=2.0
pyyaml>=6.0

# Labeling tool (labeling/): Files-API client + local web UI. No torch needed
# for the UI, so these can live in a plain venv while SAM runs in conda.
flask>=3.0
requests>=2.31

# SAM2 (container detection) — install from source (no stable PyPI release):
# pip install 'git+https://github.com/facebookresearch/segment-anything-2.git'
# Then download a checkpoint into checkpoints/:
#   wget https://dl.fbaipublicfiles.com/segment_anything_2/072824/sam2_hiera_tiny.pt -P checkpoints/

# Dataset pipeline (dataset_tools/ + dataset_pipeline.py): master -> YOLO/semantic/COCO.
# opencv-python + numpy + pyyaml (above) already cover the core; these add UX/extras.
pillow>=10.0
tqdm>=4.66
matplotlib>=3.7
# Optional — only needed for RLE round-trips / COCO eval downstream (polygon
# export itself does NOT require it):
# pycocotools>=2.0.7

# Dev / testing
pytest>=7.0
