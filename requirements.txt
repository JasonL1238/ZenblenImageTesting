opencv-python>=4.8
numpy>=1.24
torch>=2.0
pydantic>=2.0
anthropic>=0.40
pyyaml>=6.0

# SAM2 — install from source (no stable PyPI release):
# pip install 'git+https://github.com/facebookresearch/segment-anything-2.git'
# Then download a checkpoint into checkpoints/:
#   wget https://dl.fbaipublicfiles.com/segment_anything_2/072824/sam2_hiera_tiny.pt -P checkpoints/

# SegFormer — install when you have labeled data to fine-tune:
# pip install transformers datasets

# Dev / testing
pytest>=7.0
